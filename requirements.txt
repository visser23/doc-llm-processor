# Fast processor (recommended)
PyMuPDF>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# Optional: Word document support
python-docx>=1.1.0

# Optional: PowerPoint support
python-pptx>=0.6.23

# Legacy processor (uses Unstructured.io — heavier dependencies, OCR-based)
# Uncomment if you need the legacy processor (process_documents.py):
# unstructured[all-docs]>=0.18.11
# python-dotenv>=1.0.1
